<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <%- include('./partials/navbar') %>
    <h1>We are rendering EJS Homepage!</h1>
    <% let user = "user" %>
    <% let name = "Hasan" %>

    <% if(user === "admin"){ %>
        <p>WELCOME ADMIN USER: <%= name %></p>
    <%} else{ %>
        <p>WELCOME NORMAL USER NOT ADMIN: <%= name %></p>
    <% } %>

    <h1>Your Course is <%= course %></h1>
    
</body>
</html>